const createTriangleToSquareWaveGif = (amp, freq, fileName) => {
    console.log(`Amplitude is ${amp}, frequency is ${freq} and fileName is ${fileName}`)
}

module.exports = createTriangleToSquareWaveGif
